# ============================================================
# ai-auto-publisher 环境变量配置
# 复制此文件为 .env 并填入实际值
# ============================================================

# --- 服务配置 ---
API_HOST=0.0.0.0
API_PORT=8000
MCP_PORT=8080
DATABASE_URL=sqlite:///./data/publisher.db

# --- 微信公众号 ---
# 获取步骤:
#   1. 正式号: 登录 https://mp.weixin.qq.com → 左侧菜单「设置与开发」→「基本配置」
#      - AppID(应用ID) 和 AppSecret(应用密钥) 就在页面上
#      - 首次使用需点击「重置」生成 AppSecret，请立即保存（只显示一次）
#      - 还需在「IP白名单」中添加服务器公网 IP
#   2. 测试号(推荐开发调试):
#      - 访问 https://mp.weixin.qq.com/debug/cgi-bin/sandbox?t=sandbox/login
#      - 微信扫码即可获取测试 AppID 和 AppSecret（无需公众号）
#      - 测试号支持所有接口，无需审核，但仅测试号关注者可见
WECHAT_MP_APP_ID=your_app_id
WECHAT_MP_APP_SECRET=your_app_secret

# --- Twitter/X API v2 ---
TWITTER_API_KEY=your_api_key
TWITTER_API_SECRET=your_api_secret
TWITTER_ACCESS_TOKEN=your_access_token
TWITTER_ACCESS_TOKEN_SECRET=your_access_token_secret

# --- Wechatsync MCP ---
# Wechatsync 通过 Chrome Extension + WebSocket Bridge 工作，无需 HTTP URL
# 获取 TOKEN 步骤:
#   1. 安装 Chrome 扩展: https://chrome.google.com/webstore/detail/hchobocdmclopcbnibdnoafilagadion
#      (或手动安装: 下载 zip → chrome://extensions → 开发者模式 → 加载已解压的扩展)
#   2. 点击扩展图标 → 设置 → 找到「MCP 连接」→ 开启并设置 Token
#   3. 将 Token 填入下方
# MCP Server 已安装在: /Users/yupeng/wechatsync-v2/packages/mcp-server/dist/index.js
# CLI 工具全局可用: wechatsync sync article.md -p zhihu,juejin,csdn
WECHATSYNC_TOKEN=your_wechatsync_token
WECHATSYNC_MCP_URL=http://localhost:9529

# --- Knot 平台 ---
KNOT_API_TOKEN=your_knot_api_token

# --- Playwright ---
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_SLOW_MO=1000
